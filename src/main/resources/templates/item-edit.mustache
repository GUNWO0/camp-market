<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/style.css">
    <title>물품 수정</title>
</head>
<body>
<div class="container">
    <h1>✏️ 물품 수정</h1>
    <form action="/items/{{item.id}}/edit" method="post" class="form-box">
        <input type="text" name="title" value="{{item.title}}" required/>
        <input type="number" name="price" value="{{item.price}}" required/>
        <select name="status">
            <option value="ON_SALE" {{#isOnSale}}selected{{/isOnSale}}>판매중</option>
            <option value="RESERVED" {{#isReserved}}selected{{/isReserved}}>예약중</option>
            <option value="SOLD_OUT" {{#isSoldOut}}selected{{/isSoldOut}}>판매완료</option>
        </select>
        <textarea name="description">{{item.description}}</textarea>
        <button type="submit">수정 완료</button>
    </form>
    <a href="/items/{{item.id}}" class="back-link">← 상세 보기</a>
</div>
</body>
</html>
